# Build artifacts
.build/
# xcodebuild -derivedDataPath output used by scripts/build-app.sh and local builds.
build/
# Mermaid renderer: built by scripts/build-mermaid.sh, not committed.
# rust/mermaid-shim/Cargo.lock IS committed — merman is a pinned pre-1.0 alpha.
Frameworks/
rust/mermaid-shim/target/
DerivedData/
*.xcodeproj/xcuserdata/
*.xcodeproj/project.xcworkspace/xcuserdata/

# macOS
.DS_Store

# SPM
.swiftpm/
Package.resolved

# Xcode
*.pbxuser
!default.pbxuser
*.mode1v3
!default.mode1v3
*.mode2v3
!default.mode2v3
*.perspectivev3
!default.perspectivev3
xcuserdata/

# Local signing config
Local.xcconfig

# Context files
.context/
